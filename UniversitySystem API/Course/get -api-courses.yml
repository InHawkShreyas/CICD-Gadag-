info:
  name: get /api/courses
  type: http
  seq: 2
  tags:
    - Course

http:
  method: GET
  url: "{{baseUrl}}/api/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/courses"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
