info:
  name: get /api/AdmittedStudent
  type: http
  seq: 2
  tags:
    - AdmittedStudent

http:
  method: GET
  url: "{{baseUrl}}/api/AdmittedStudent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AdmittedStudent"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
