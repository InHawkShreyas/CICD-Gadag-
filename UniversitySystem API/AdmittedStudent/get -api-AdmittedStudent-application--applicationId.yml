info:
  name: get /api/AdmittedStudent/application/:applicationId
  type: http
  seq: 4
  tags:
    - AdmittedStudent

http:
  method: GET
  url: "{{baseUrl}}/api/AdmittedStudent/application/:applicationId"
  params:
    - name: applicationId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AdmittedStudent/application/:applicationId"
      method: GET
      params:
        - name: applicationId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
