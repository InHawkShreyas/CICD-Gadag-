info:
  name: get /api/SeatType
  type: http
  seq: 2
  tags:
    - SeatType

http:
  method: GET
  url: "{{baseUrl}}/api/SeatType"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/SeatType"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
