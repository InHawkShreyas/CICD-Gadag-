info:
  name: post /api/SeatType
  type: http
  seq: 1
  tags:
    - SeatType

http:
  method: POST
  url: "{{baseUrl}}/api/SeatType"
  body:
    type: json
    data: |-
      {
        "applicationId": "",
        "applicationNo": "",
        "seatTypeId": "",
        "seatTypeName": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/SeatType"
      method: POST
      body:
        type: json
        data: |-
          {
            "applicationId": "",
            "applicationNo": "",
            "seatTypeId": "",
            "seatTypeName": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
