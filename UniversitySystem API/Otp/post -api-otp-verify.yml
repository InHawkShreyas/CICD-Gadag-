info:
  name: post /api/otp/verify
  type: http
  seq: 2
  tags:
    - Otp

http:
  method: POST
  url: "{{baseUrl}}/api/otp/verify"
  body:
    type: json
    data: |-
      {
        "username": "",
        "otp": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/otp/verify"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "otp": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
