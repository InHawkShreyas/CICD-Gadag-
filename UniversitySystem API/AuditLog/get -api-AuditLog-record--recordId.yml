info:
  name: get /api/AuditLog/record/:recordId
  type: http
  seq: 2
  tags:
    - AuditLog

http:
  method: GET
  url: "{{baseUrl}}/api/AuditLog/record/:recordId"
  params:
    - name: recordId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AuditLog/record/:recordId"
      method: GET
      params:
        - name: recordId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
