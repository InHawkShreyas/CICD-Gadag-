info:
  name: post /api/AcademicYear
  type: http
  seq: 1
  tags:
    - AcademicYear

http:
  method: POST
  url: "{{baseUrl}}/api/AcademicYear"
  body:
    type: json
    data: |-
      {
        "description": "",
        "startDate": "",
        "endDate": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AcademicYear"
      method: POST
      body:
        type: json
        data: |-
          {
            "description": "",
            "startDate": "",
            "endDate": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
