info:
  name: get /api/FeeCollection
  type: http
  seq: 2
  tags:
    - FeeCollection

http:
  method: GET
  url: "{{baseUrl}}/api/FeeCollection"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/FeeCollection"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
