info:
  name: get /api/FeeCollection/:id
  type: http
  seq: 4
  tags:
    - FeeCollection

http:
  method: GET
  url: "{{baseUrl}}/api/FeeCollection/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/FeeCollection/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
