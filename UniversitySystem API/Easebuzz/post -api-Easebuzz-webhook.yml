info:
  name: post /api/Easebuzz/webhook
  type: http
  seq: 2
  tags:
    - Easebuzz

http:
  method: POST
  url: "{{baseUrl}}/api/Easebuzz/webhook"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/Easebuzz/webhook"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
