info:
  name: get /api/CustomerSupport
  type: http
  seq: 2
  tags:
    - CustomerSupport

http:
  method: GET
  url: "{{baseUrl}}/api/CustomerSupport"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/CustomerSupport"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
