info:
  name: post /api/CustomerSupport
  type: http
  seq: 1
  tags:
    - CustomerSupport

http:
  method: POST
  url: "{{baseUrl}}/api/CustomerSupport"
  body:
    type: json
    data: |-
      {
        "username": "",
        "issue": "",
        "description": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/CustomerSupport"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "issue": "",
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
