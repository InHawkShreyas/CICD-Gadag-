info:
  name: post /api/login
  type: http
  seq: 2
  tags:
    - Login

http:
  method: POST
  url: "{{baseUrl}}/api/login"
  body:
    type: json
    data: |-
      {
        "username": "",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
