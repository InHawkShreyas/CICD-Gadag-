info:
  name: post /api/login/reset-password
  type: http
  seq: 3
  tags:
    - Login

http:
  method: POST
  url: "{{baseUrl}}/api/login/reset-password"
  body:
    type: json
    data: |-
      {
        "username": "",
        "newPassword": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/login/reset-password"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "newPassword": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
