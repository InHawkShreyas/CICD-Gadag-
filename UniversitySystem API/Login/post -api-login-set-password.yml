info:
  name: post /api/login/set-password
  type: http
  seq: 1
  tags:
    - Login

http:
  method: POST
  url: "{{baseUrl}}/api/login/set-password"
  body:
    type: json
    data: |-
      {
        "username": "",
        "password": "",
        "registrationId": "",
        "roleId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/login/set-password"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "password": "",
            "registrationId": "",
            "roleId": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
