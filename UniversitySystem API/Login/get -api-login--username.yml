info:
  name: get /api/login/:username
  type: http
  seq: 4
  tags:
    - Login

http:
  method: GET
  url: "{{baseUrl}}/api/login/:username"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/login/:username"
      method: GET
      params:
        - name: username
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
