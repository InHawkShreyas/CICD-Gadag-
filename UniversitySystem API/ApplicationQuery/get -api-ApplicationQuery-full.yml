info:
  name: get /api/ApplicationQuery/full
  type: http
  seq: 1
  tags:
    - ApplicationQuery

http:
  method: GET
  url: "{{baseUrl}}/api/ApplicationQuery/full"
  params:
    - name: appNo
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/ApplicationQuery/full"
      method: GET
      params:
        - name: appNo
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
