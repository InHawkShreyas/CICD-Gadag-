info:
  name: post /api/ApplicationQuery/filter
  type: http
  seq: 2
  tags:
    - ApplicationQuery

http:
  method: POST
  url: "{{baseUrl}}/api/ApplicationQuery/filter"
  body:
    type: json
    data: |-
      {
        "degreeId": "",
        "courseId": "",
        "categoryId": "",
        "academicYearId": "",
        "seatTypeId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/ApplicationQuery/filter"
      method: POST
      body:
        type: json
        data: |-
          {
            "degreeId": "",
            "courseId": "",
            "categoryId": "",
            "academicYearId": "",
            "seatTypeId": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
