info:
  name: get /api/Notification
  type: http
  seq: 3
  tags:
    - Notification

http:
  method: GET
  url: "{{baseUrl}}/api/Notification"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/Notification"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
