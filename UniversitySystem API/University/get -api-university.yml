info:
  name: get /api/university
  type: http
  seq: 2
  tags:
    - University

http:
  method: GET
  url: "{{baseUrl}}/api/university"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/university"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
