info:
  name: get /api/AcademicDate
  type: http
  seq: 2
  tags:
    - AcademicDate

http:
  method: GET
  url: "{{baseUrl}}/api/AcademicDate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AcademicDate"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
