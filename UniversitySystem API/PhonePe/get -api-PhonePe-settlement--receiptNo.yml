info:
  name: get /api/PhonePe/settlement/:receiptNo
  type: http
  seq: 4
  tags:
    - PhonePe

http:
  method: GET
  url: "{{baseUrl}}/api/PhonePe/settlement/:receiptNo"
  params:
    - name: receiptNo
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/PhonePe/settlement/:receiptNo"
      method: GET
      params:
        - name: receiptNo
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
