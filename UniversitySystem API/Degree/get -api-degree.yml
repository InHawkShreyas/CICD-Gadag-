info:
  name: get /api/degree
  type: http
  seq: 2
  tags:
    - Degree

http:
  method: GET
  url: "{{baseUrl}}/api/degree"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/degree"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
