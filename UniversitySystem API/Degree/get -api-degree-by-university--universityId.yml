info:
  name: get /api/degree/by-university/:universityId
  type: http
  seq: 4
  tags:
    - Degree

http:
  method: GET
  url: "{{baseUrl}}/api/degree/by-university/:universityId"
  params:
    - name: universityId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/degree/by-university/:universityId"
      method: GET
      params:
        - name: universityId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
