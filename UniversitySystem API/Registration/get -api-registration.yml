info:
  name: get /api/registration
  type: http
  seq: 2
  tags:
    - Registration

http:
  method: GET
  url: "{{baseUrl}}/api/registration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/registration"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
