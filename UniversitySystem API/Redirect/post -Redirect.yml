info:
  name: post /Redirect
  type: http
  seq: 2
  tags:
    - Redirect

http:
  method: POST
  url: "{{baseUrl}}/Redirect"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/Redirect"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
