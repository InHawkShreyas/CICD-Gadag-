info:
  name: get /api/Student
  type: http
  seq: 2
  tags:
    - Student

http:
  method: GET
  url: "{{baseUrl}}/api/Student"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/Student"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
