info:
  name: get /api/AdmissionFeeStructure
  type: http
  seq: 2
  tags:
    - AdmissionFeeStructure

http:
  method: GET
  url: "{{baseUrl}}/api/AdmissionFeeStructure"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/AdmissionFeeStructure"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
