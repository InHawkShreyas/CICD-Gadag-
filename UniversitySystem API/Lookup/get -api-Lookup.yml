info:
  name: get /api/Lookup
  type: http
  seq: 1
  tags:
    - Lookup

http:
  method: GET
  url: "{{baseUrl}}/api/Lookup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/Lookup"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
