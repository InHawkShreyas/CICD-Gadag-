info:
  name: get /api/application-documents
  type: http
  seq: 3
  tags:
    - ApplicationDocument

http:
  method: GET
  url: "{{baseUrl}}/api/application-documents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}/api/application-documents"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
