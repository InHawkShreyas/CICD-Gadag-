# Audit Log API

- Base route: `api/AuditLog` (controller uses `api/[controller]`)

Endpoints:
- `GET /api/AuditLog` — Get all audit logs (authorized).
- `GET /api/AuditLog/record/{recordId}` — Get audit logs by record ID.
