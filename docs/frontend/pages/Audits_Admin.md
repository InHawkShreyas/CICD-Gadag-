# Audits (Admin)

- Route: `/admin/audits`
- Source: `frontend/web/src/features/admin/audits.tsx`

Purpose:
- View audit logs and changes tracked by the system.

Key behaviors:
- Fetches audit logs via `auditService` and displays them with filters.
