# Support (Admin)

- Route: `/admin/support`
- Source: `frontend/web/src/features/admin/support-admin.tsx`

Purpose:
- Admin interface to manage support tickets raised by students and users.

Key behaviors:
- Uses `customerSupportService` to list tickets, assign, resolve and add remarks.
