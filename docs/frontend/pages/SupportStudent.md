# Student Support Page

- Route: `/student/support`
- Source: `frontend/web/src/features/student/support-student.tsx`

Purpose:
- Allow students to create and view support tickets related to admissions or payments.

Key behaviors:
- Create ticket via `customerSupportService.createTicket()` and list tickets via `customerSupportService.getTicketsByUsername()`.
