# Admin Fee Receipt (Admin)

- Route: `/admin/fee-receipt`
- Source: `frontend/web/src/features/admin/admin_fee_reciept.tsx`

Purpose:
- Render and print fee receipts generated or entered by administrators.

Key behaviors:
- Uses `feeCollectionService` and receipt generation helpers to produce printable receipts.
